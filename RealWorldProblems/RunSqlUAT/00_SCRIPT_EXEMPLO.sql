----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------

----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
---- PRE EXECUTION ---------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
SET LINESIZE 10000;
SET TIMING ON;
SET SERVEROUTPUT ON SIZE UNLIMITED;
SPOOL c:/temp/00_SCRIPT_EXEMPLO.log;
WHENEVER SQLERROR EXIT FAILURE;

ALTER SESSION SET CURRENT_SCHEMA=REPORTWEB;

----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
---- EXECUTION -------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
CREATE TABLE "ABC_CDE"
  (    "NAME" VARCHAR2(255) NOT NULL ENABLE,
       "XPTO" VARCHAR2(10) NOT NULL ENABLE,
       "DESCRIPTION" VARCHAR2(500) NOT NULL ENABLE,
       "ABS" VARCHAR2(20) NOT NULL ENABLE,
       "STATUS" NUMBER(1,0) DEFAULT 0 NOT NULL ENABLE,
       "EXECUTION_DATE" TIMESTAMP (6) DEFAULT SYSTIMESTAMP NOT NULL ENABLE,
       "USER_NAME" VARCHAR2(30) NOT NULL ENABLE,
       "OS_USER" VARCHAR2(32) NOT NULL ENABLE,
       "OS_HOST" VARCHAR2(255) NOT NULL ENABLE,
        CONSTRAINT "STATUS_CHK" CHECK (STATUS IN (0,1)) ENABLE,
        CONSTRAINT "ABC_CDE_PK" PRIMARY KEY ("NAME", "APPLICATION_VERSION")
)
;

INSERT INTO ABC_CDE (NAME, XPTO, DESCRIPTION, ABS, USER_NAME, OS_USER, OS_HOST)
VALUES(
  'AAAAAAAAAAAAAA',
  '1117.11',
  'Sbrublhouws',  
  'ASDASDASDASD',
  (SELECT SYS_CONTEXT ('USERENV', 'SESSION_USER') FROM DUAL),
  (SELECT SYS_CONTEXT ('USERENV', 'OS_USER') FROM DUAL),
  (SELECT SYS_CONTEXT ('USERENV', 'HOST') FROM DUAL)
);

COMMIT;

----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
---- FINISH EXECUTION ------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------
spool off;